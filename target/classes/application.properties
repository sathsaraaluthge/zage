spring.application.name=zage

server.servlet.context-path=/zage

spring.datasource.url=jdbc:mysql://localhost:3306/zage?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Set this to false during development to bypass JWT authentication
app.security.enabled=false